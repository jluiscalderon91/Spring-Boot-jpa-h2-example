package com.javabydeveloper.demo;

public class Spring_boot_H2_test {

	/*@Autowired
	private TipoCambioRepository userRepository;
	
	@Test
	@DisplayName("Create User Test ")
	void createUserTest() {
		
		List<TipoCambio> created = userRepository.saveAll(getTipoCambio());
		
		assertTrue(created != null);
	}
	
	private List<TipoCambio> getTipoCambio() {

		List<TipoCambio> tipoCambios = new ArrayList<>();
		TipoCambio tipoCambio = new TipoCambio();
		tipoCambio.setMonedaOrigen(1);
		tipoCambio.setMonedaDestino(2);
		tipoCambio.setCreationTime(new Date());
		tipoCambios.add(tipoCambio);
		
		return tipoCambios;
	}*/
}
